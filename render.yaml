services:
  - type: web
    name: college-discovery-backend
    env: node
    rootDir: backend
    plan: free
    nodeVersion: 20
    buildCommand: npm install && npm run build
    startCommand: npm run prisma:migrate:deploy && npm run prisma:seed && npm start
